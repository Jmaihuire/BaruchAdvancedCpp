#pragma once

class Subject;

//Updated to fit the UML diagram
class DoubleFormat
{
public:
    static void Update(Subject* subject); // only gets called if theres a change...hence the name update :) 
};

